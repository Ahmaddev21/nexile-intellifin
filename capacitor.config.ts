import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nexile.intellifin',
  appName: 'Nexile Intellifin',
  webDir: 'dist'
};

export default config;
